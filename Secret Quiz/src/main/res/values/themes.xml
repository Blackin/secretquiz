<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="BlackImageBackground" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Action Bar -->
        <item name="actionBarStyle">@style/ActionBarSecretQuiz</item>
        <item name="android:actionBarStyle">@style/ActionBarSecretQuiz</item>

        <!-- Activity -->
        <item name="android:background">@drawable/original_bk_activities</item>
    </style>

    <!-- Action Bar -->
    <style name="ActionBarSecretQuiz" parent="Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@drawable/correct_button_bg</item>
        <item name="background">@drawable/correct_button_bg</item>
    </style>

</resources>